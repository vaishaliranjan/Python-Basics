#Write a program to use string.format() method to format the following the
#variables as per the expected output
totalMoney = 1000
quantity = 3
price = 450

stmt="I have {0} dollars so I can buy {1} football for {2:.2f} dollars"

print(stmt.format(totalMoney, quantity, price))

# %.2f couldnt be applied to int so.2f
